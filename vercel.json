{
    "buildCommand": "npm run build:dev && node build-vercel.js",
    "outputDirectory": "dist/client",
    "rewrites": [
          {
                  "source": "/(.*)",
                  "destination": "/index.html"
          }
    ]
}
