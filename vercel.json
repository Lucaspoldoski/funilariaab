{
        "buildCommand": "npm run build && node build-vercel.js",
        "outputDirectory": "dist/client",
        "rewrites": [
                  {
                              "source": "/(.*)",
                              "destination": "/index.html"
                  }
        ]
}
